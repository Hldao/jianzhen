# 训练页 UX 优化记录 · 2026-06-07

> 反馈源：团队飞书《箭证小程序使用反馈》
> 范围：`pages/training/`（训练流程）+ `pages/mine/`（成就抽屉 bug）

---

## ✅ 已完成（9 项）

| 编号 | 反馈问题 | 改动内容 | 涉及文件 |
|---|---|---|---|
| 1 | 初始默认值过于小众（70m / 122cm 为户外专业规格） | 默认改 **18m / 40cm**（室内大众标准），首屏即显示靶面提示 | `training.js` |
| 2.a | 空拉「次」字孤零零卡在圆圈右下角 | 圆环改纵向布局，「次」移到「2/6」**正下方居中** | `training.wxml` / `.wxss` |
| 2.b | 「记录一支」按钮太细长，射箭戴护指难点按 | 新增专属 `.btn-spt-record`，高度 `96 → 180rpx`，圆角字号同步加大 | `training.wxml` / `.wxss` |
| 2.c | 撒放倒计时偏小、不够显眼 | 计时中**放大占屏 + 实心填充底色 + 白字 140rpx**；最后 15 秒**整圈闪烁**；结束震动 `vibrateShort → vibrateLong` | `training.wxml` / `.wxss` / `.js` |
| 2.d | tips 夹带英文备注 | 去掉 `（follow-through）` | `training.wxml` |
| 3.a | 「就位后点击」像非按钮、位置偏僻；开始前长横条无意义 | 未开始态换**全宽橙色大启动按钮** `.timer-start-btn`，开始后才显示时间 + 进度条 | `training.wxml` / `.wxss` |
| 3.b-1 | 计时条太细 | 改成**实心填充整条**（64rpx 高，进度作半透明状态色背景，时间/标签叠上层），不额外占高度 | `training.wxml` / `.wxss` |
| 3.b-2 | 「撤销 / 0-6 支 / 内金」字重过轻字号小；`← →` 字符箭头显得原始 | 统一加粗增大；撤销/下一组的字符箭头换 **lc 图标**（`lc-circle-x` / `lc-chevron-right`） | `training.wxml` / `.wxss` |
| 3.b-4 | 🐛 「我的」页成就抽屉底部被 tabBar 遮挡、显示不全 | `.ach-detail-card` 底部 padding 补 `50px`（自定义 tabBar 高度 = 50px + 安全区） | `mine.wxss` |

---

## 🔴 待办 / 待定

| 编号 | 事项 | 说明 |
|---|---|---|
| 3.b-3 | **分享生成图重设计**（大工程） | `training.js` 的 `_renderMomentCanvas`（完美组 / 收黄 / PB 三主题 canvas）默认字号字重、缺设计。分享是关键环节，需重新设计一版（设计规范 / 视觉焦点 / 观众理解）。⚠ canvas 无法本地预览，需在小程序内生成截图来回打磨 |
| 2.d-补 | `SPT` 英文缩写是否保留 | 标题「空拉练习（SPT）」共 3 处。倾向保留（射箭圈通用术语），待确认 |

---

## 技术备注

- **震动**：`vibrateLong` 仅真机有效，开发者工具模拟器无反馈。
- **遗留死 CSS**（可后续清理）：`training.wxss` 中 `.timer-ready` / `.timer-start-hint` / `.timer-track` 已不再被引用。
- **验证方式**：微信开发者工具编译预览；震动/手感需真机扫预览码。
